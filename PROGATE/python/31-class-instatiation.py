# As shown below, if we call ClassName() we can create an instance of that class (blueprint). Also, using variable_name = ClassName(), we can assign the new instance to a variable.

class MenuItem:
    pass

# Create an instance of the MenuItem class
menu_item1 = MenuItem()