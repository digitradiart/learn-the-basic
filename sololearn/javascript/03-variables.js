// variables are containers for storing data values
// the value from variable can change throughout the program
// declaring a variable is as simple as using the keyword var.
// In Javascript,  the equal sign (=) is called 'assignment operator'
// Javascript is case-sensitive
// every written 'instruction' is called a statement. Javascript statement are separated by semicolon

var x = 10;
document.write(x);

var my_variable = 32;
console.log(my_variable );

// Brace yourself for more rules!
    // the first character of a variable name must be a letter, underscore, or a dollar sign ($). Subsequent characters can be letters, digits, underscores. or dollar sign.
    // The first character of a variable can't be a number.
    // // Can't include mathematical or logical operator in their name. For instance, 2*something or this+that.
    // Can't contain spaces.
    // We are not allowed to use any special symbols, like my#num, num%, etc.

